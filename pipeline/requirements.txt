# DSRP Knowledge Ingestion Pipeline Dependencies
# Install with: pip install -r requirements.txt

# =============================================================================
# CORE DEPENDENCIES
# =============================================================================

# MongoDB client for episodic memory (vector storage)
pymongo>=4.6.0

# TypeDB client for semantic memory (knowledge graph)
typedb-driver>=2.28.0

# LLM SDKs (supports multiple providers)
anthropic>=0.39.0
google-generativeai>=0.8.0
openai>=1.0.0

# =============================================================================
# TEXT PROCESSING
# =============================================================================

# PDF extraction
pypdf>=4.0.0

# Text chunking (from LangChain)
langchain>=0.1.0
langchain-text-splitters>=0.0.1

# =============================================================================
# EMBEDDINGS (Local, no API key needed)
# =============================================================================

# Sentence Transformers for local embeddings
sentence-transformers>=2.2.0

# PyTorch (required by sentence-transformers)
# Will install CPU version by default
torch>=2.0.0

# NumPy for vector operations
numpy>=1.24.0

# =============================================================================
# UTILITIES
# =============================================================================

# JSON Schema validation
jsonschema>=4.20.0

# Environment variable handling
python-dotenv>=1.0.0

# Progress bars (optional, for long documents)
tqdm>=4.66.0
